#include <stdio.h>
int main() 
{
    int x;
    scanf("%d",&x);
    for(int i=1;i<=x;i++)
    {
        c==0
        if(x%i==0)
        {
            // printf("Prime");
            // break;
            c==1
        }
        if(c==0)
        {
            printf("Not prime");
        }
        else
        {
            printf("Prime");
        }
    }
    return 0;
}