#include <stdio.h>
int main() 
{
    int x;
    scanf("%d",&x);
    for(int 1=1;i<=x;i++)
    {
        if(x%i==0)
        [
            printf("Prime");
            break;
        ]
        else
        {
            printf("Not prime");
        }
    }
    return 0;
}